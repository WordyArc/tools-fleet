# Tool Finder

A Fleet-inspired, keyboard-driven popup for finding and opening IntelliJ Platform tool windows.

Press `Ctrl+Shift+T` (`Cmd+Shift+T` on macOS), type to filter, use `Up` and `Down` to move, and press `Enter` to open the selected tool window.
